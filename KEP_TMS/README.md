# setup instructions
1. Clone the repository
2. Navigate to the project directory: cd kep_tms
3. Install dependencies: npm install  
4. To run the front-end application locally, execute: npm run dev 
5. Then open http://localhost:5173 in your browser.